export class TouchControls{
 constructor({joy,stick,camPad,run,action,shoot}){this.move={x:0,y:0};this.cameraDelta=0;this.running=false;this.onAction=()=>{};this.onShoot=()=>{};let joyId=null,camId=null,camX=0;const update=e=>{const r=joy.getBoundingClientRect();let dx=e.clientX-(r.left+r.width/2),dy=e.clientY-(r.top+r.height/2),max=r.width*.34,m=Math.hypot(dx,dy);if(m>max){dx*=max/m;dy*=max/m}this.move.x=dx/max;this.move.y=dy/max;stick.style.transform=`translate(${dx}px,${dy}px)`};joy.addEventListener('pointerdown',e=>{e.preventDefault();joyId=e.pointerId;joy.setPointerCapture(e.pointerId);update(e)});joy.addEventListener('pointermove',e=>{if(e.pointerId===joyId)update(e)});const release=e=>{if(e.pointerId===joyId){joyId=null;this.move.x=this.move.y=0;stick.style.transform='translate(0,0)'}};joy.addEventListener('pointerup',release);joy.addEventListener('pointercancel',release);camPad.addEventListener('pointerdown',e=>{camId=e.pointerId;camX=e.clientX;camPad.setPointerCapture(e.pointerId)});camPad.addEventListener('pointermove',e=>{if(e.pointerId===camId){this.cameraDelta+=e.clientX-camX;camX=e.clientX}});const camRelease=e=>{if(e.pointerId===camId)camId=null};camPad.addEventListener('pointerup',camRelease);camPad.addEventListener('pointercancel',camRelease);run.addEventListener('pointerdown',()=>this.running=true);run.addEventListener('pointerup',()=>this.running=false);run.addEventListener('pointercancel',()=>this.running=false);action.addEventListener('pointerdown',()=>this.onAction());shoot.addEventListener('pointerdown',()=>this.onShoot())}
 consumeCamera(){const d=this.cameraDelta;this.cameraDelta=0;return d}
}
